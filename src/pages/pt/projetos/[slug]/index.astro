---
import { mockProjects } from "@/lib/cms";

export async function getStaticPaths() {
  return mockProjects.map((project) => ({
    params: { slug: project.slug },
  }));
}

return Astro.redirect("/pt#projetos");
---
